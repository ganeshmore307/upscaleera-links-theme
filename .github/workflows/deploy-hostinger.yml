name: Deploy UpscaleEra Theme to Hostinger

on:
  push:
    branches:
      - main
    paths:
      - 'functions.php'
      - 'style.css'
      - 'front-page.php'
      - 'index.php'
      - 'inc/**'
      - 'assets/**'
      - 'mu-plugin/**'
      - '.github/workflows/deploy-hostinger.yml'
  workflow_dispatch:

jobs:
  deploy:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Deploy theme to Hostinger via FTP
        uses: SamKirkland/FTP-Deploy-Action@v4.3.5
        with:
          server: ${{ secrets.FTP_SERVER }}
          username: ${{ secrets.FTP_USERNAME }}
          password: ${{ secrets.FTP_PASSWORD }}
          protocol: ftp
          port: 21
          local-dir: ./
          server-dir: ${{ secrets.FTP_SERVER_DIR }}
          dangerous-clean-slate: false
          exclude: |
            **/.git*
            **/.git*/**
            **/.github/**
            main-site-mu-plugin/**
            mu-plugin/**
            README.md

      - name: Deploy Elementor Home Bridge as MU plugin
        uses: SamKirkland/FTP-Deploy-Action@v4.3.5
        with:
          server: ${{ secrets.FTP_SERVER }}
          username: ${{ secrets.FTP_USERNAME }}
          password: ${{ secrets.FTP_PASSWORD }}
          protocol: ftp
          port: 21
          local-dir: ./mu-plugin/
          server-dir: /public_html/wp-content/mu-plugins/
          dangerous-clean-slate: false
