document.addEventListener('DOMContentLoaded',function(){const items=document.querySelectorAll('.ue-reveal');if(!items.length)return;if(!('IntersectionObserver'in window)){items.forEach(function(el){el.classList.add('ue-in')});return}const observer=new IntersectionObserver(function(entries){entries.forEach(function(entry){if(entry.isIntersecting){entry.target.classList.add('ue-in');observer.unobserve(entry.target)}})},{threshold:.08});items.forEach(function(el,index){el.style.transitionDelay=Math.min(index*45,220)+'ms';observer.observe(el)})});
